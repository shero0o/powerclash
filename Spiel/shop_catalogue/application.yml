spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shop_catalogue_db
    username: shop_catalogue_user
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
